<Page x:Class="up.Pages.Products"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:up.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Products" Background="White">

    <Grid>
        <Grid Background="DarkOrange" VerticalAlignment="Top">
            <Button Content="Вернуться" Click="Back" Style="{StaticResource RoundedButtonStyle}"  Background="White" Foreground="Black" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0" Height="25" Width="100"/>
            <Label Foreground="White" x:Name="navName" Content="Товары" Margin="0,10,0,10" FontFamily="Malgun Gothic" FontSize="30" VerticalAlignment="Top" HorizontalAlignment="Center"/>
        </Grid>
        <Grid>

            <DataGrid AlternationCount="1" x:Name="productsDataGrid" Margin="20,80,20,52" AutoGenerateColumns="False" CanUserAddRows="True" CanUserDeleteRows="True" SelectionMode="Single" GridLinesVisibility="Horizontal" RowBackground="White" AlternatingRowBackground="LightGray">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Артикул" Binding="{Binding Article}" Width="1*"/>
                    <DataGridTextColumn Header="Название" Binding="{Binding NameProduct}" Width="1*"/>
                    <DataGridTextColumn Header="Производитель" Binding="{Binding Brand}" Width="1*"/>
                    <DataGridTextColumn Header="Модель" Binding="{Binding Model}" Width="1*"/>
                    <DataGridTextColumn Header="Цена" Binding="{Binding Price}" Width="1*"/>
                    <DataGridTextColumn Header="Остаток" Binding="{Binding QuantityStock}" Width="1*"/>
                    <DataGridTextColumn Header="Категория" Binding="{Binding CategoryName}" Width="1*"/>
                </DataGrid.Columns>
            </DataGrid>
            <Button Content="Применить изменения" Click="AcceptChange" Style="{StaticResource RoundedButtonStyle}" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="20,0,0,10" Height="25" Width="139"/>
        </Grid>
    </Grid>
</Page>
